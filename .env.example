# --- LinkedIn session (required for profile fetches; leave blank to run with sessionValid=false) ---
LI_AT=
JSESSIONID=

# --- API auth ---
# If unset, the API is open to anyone. Do not deploy that way.
API_KEY=

# --- Cache ---
# If unset, falls back to an in-process Map cache.
REDIS_URL=
